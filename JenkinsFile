node('local-node') {
    stage 'Clone'
    git("ssh://afahd@gerrit.plumgrid.com:29418/builder")
    stage 'Pipeline'
    withEnv(["WORKSPACE=${pwd()}","JENKINS_BUILDER_SCRIPT_DIR=${pwd()}"]) {
        sh '$WORKSPACE/jenkins-run-pipelines.sh -p devstack -b master'
    }
}
